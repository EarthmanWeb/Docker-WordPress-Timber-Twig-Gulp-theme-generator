{% extends "base.twig" %}

{% block content %}
	<h2 class="mt-3">
		{{ page.title }}
		{% if user %}
			<a class="float-right font-size-20" href="{{ post.edit_link }}">{{ _e('edit') }}</a>
		{% endif %}
	</h2>
	<p class="mt-3">
		{{ page.content }}
	</p>
{% endblock %}
